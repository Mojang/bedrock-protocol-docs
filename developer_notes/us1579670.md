# PresenceConfiguration richPresenceId
- Added `richPresenceId` string field to `PresenceConfiguration`.
- The field is optional (empty string means no override). When set, it allows trusted dedicated servers to drive rich presence instead of relying on client logic.
- This affects `ServerPresenceInfoPacket` and `ServerConfigurationJoinInfo` which carry `PresenceConfiguration`.
