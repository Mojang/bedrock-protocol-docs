# SerializedSkin
- Added `mProfileHash` field carrying the server-computed appearance hash used as the authoritative cache key for remote profile-image (LoD) fetches ADO:1634458
# ConnectionRequest
- Added `ProfileHash` JSON key mirroring `SerializedSkin::mProfileHash` for the initial join handshake ADO:1634458
