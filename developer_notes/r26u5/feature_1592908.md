# PlayerListPacket
- Added `PlayFab Id` to `PlayerListEntry`, alongside `XBL XUID` and `Platform Chat Id` ADO:1592908

# SerializedSkin
- Removed `PlayFab Id`, which is now carried by `PlayerListEntry` ADO:1592908
