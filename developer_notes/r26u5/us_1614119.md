# RecordStartedPacket
- Added `RecordStartedPacket`, a server to client packet carrying a `BlockPos` and a `ServerSoundHandle` to notify the client that a record started playing at a block position. ADO:1614119
