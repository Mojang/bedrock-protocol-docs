# ContainerType enum
- Added `DATA_DRIVEN_CONTAINER` for containers backed by the `minecraft:container` block component ADO:1655851

# BlockDefinition network payload
- Added the `minecraft:container` block component so clients can predict interactions with custom container blocks ADO:1655851
