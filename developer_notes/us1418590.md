# Network Protocol

* Converted `InventoryTransactionPacket` to Cereal serialization. The binary wire format has changed and is not backwards compatible. Packet fields and their semantics remain the same, but partners must update their serializer to match the new cereal binary format. ADO:1418590
