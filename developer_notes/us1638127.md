# PlayerSkinPacket
- Converted `PlayerSkinPacket` to Cereal (`CerealOnly` mode). The binary wire format has changed and is not backwards compatible with prior protocols: the trusted-skin flag is now serialized inside the shared `SerializedSkinRef` binding instead of as a standalone trailing boolean. ADO:1638127
