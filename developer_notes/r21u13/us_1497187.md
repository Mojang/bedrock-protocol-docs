# BlockCollisionBoxComponent
- Modified the network format. Removed `origin` and `size` fields. Added list named `boxes`; each box is an unnamed field containing floats of `minX`, `minY`, `minZ`, `maxX`, `maxY`, and `maxZ`. ADO:1497187