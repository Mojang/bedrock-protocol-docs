# ResourcePacksInfoPacket Cereal serialization

`ResourcePacksInfoPacket` now uses Cereal-only serialization. The reflected payload contains the pack requirement flags, world template identity/version, and resource-pack entries directly instead of the legacy manual response shape.

This is an intentional wire-format change for the protocol version that introduces this changelog entry.
