# ResourcePackClientResponsePacket
- Converted `ResourcePackClientResponsePacket` to Cereal (`CerealOnly` mode). The binary wire format has changed and is not backwards compatible with prior protocols. The `Downloading Packs` set is now bound as an optional field that is only emitted on the wire when `Response == Downloading`. ADO:1418620
