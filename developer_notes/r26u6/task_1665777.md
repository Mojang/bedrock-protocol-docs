# Network Protocol
- Client-to-server login, skin, modal form response, NPC action, and identity assertion JSON payloads with more than 64 nested objects or arrays are now rejected; wire formats are unchanged ADO:1665777
