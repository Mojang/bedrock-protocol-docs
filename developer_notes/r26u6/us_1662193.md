# ClientboundAttributeLayerSyncPacket
- Updated `EnvironmentAttributeData` to use a `Payload` variant containing `ConstantAttributeData`, `TransitionAttributeData`, or `NoiseTransitionAttributeData` ADO:1662193
- Added `ClockName` to transition settings and moved `NoiseName` from `AttributeLayerData` to `NoiseTransitionSettingsData` ADO:1662193
