# SubClientLoginPacket
- The connection request now carries a signed `PrimaryPlayFabId` claim naming the primary player the sub-client is joining under, and a sub-client login that does not match the primary on the connection it arrives on is rejected with the new `SubClientLoginNotBound` disconnect reason; the packet layout is unchanged ADO:1666295
