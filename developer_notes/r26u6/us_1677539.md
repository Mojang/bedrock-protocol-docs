# PlayerListPacketPayload
- The generated schema for `Entries` now documents its `default` value of `[]` ADO:1677539

# SetScorePacketPayload
- The generated schema for `Score Info` now documents its `default` value of `[]` ADO:1677539
