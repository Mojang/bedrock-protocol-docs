# InventoryContentPacket

- Converted `InventoryContentPacket` to Cereal. Cereal becomes the wire format under `SemanticSideBySide_LogOnMismatch`, and the `NetworkItemStackDescriptor` fields are encoded differently than in prior protocol versions. ADO:1418588
