# MapInfoRequestPacket
- Updated MapInfoRequestPacket to store only the actual pixel count during deserialization, matching the serialization logic.
- The exact index of each pixel is now stored when constructing the packet.
