# Network Protocol

* Converted `CreativeContentPacket` to Cereal-only serialization. The binary wire format has changed and is not backwards compatible.
* Group and entry data now serialize as reflected `Groups` and `Entries` vectors. Creative item categories continue to serialize as numeric enum values, and entries still reference groups by index or `CreativeItemRegistry::INVALID_INDEX` for ungrouped items.
