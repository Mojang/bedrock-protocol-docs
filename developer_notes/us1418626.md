# SetActorDataPacket
Converted `SetActorDataPacket` to `CerealOnly` serialization. The packet now uses `SynchedActorData::CopyableDataList` from `AddActorPacket`'s conversion,which serializes each Actor Data entry as `{ ID, Payload }` where `Payload` is a tagged
   variant (`Type` enum + typed `Value`).
