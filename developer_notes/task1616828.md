# PresenceConfiguration field length increase
- Increased the maximum allowed length of `PresenceConfiguration` string fields (`mExperienceName`, `mWorldName`, `mRichPresenceId`) from 40 to 50 characters.
- This affects `ServerPresenceInfoPacket` and `ServerConfigurationJoinInfo` which carry `PresenceConfiguration`.
